{
    "meta": {
        "label": "Categorical aggregation (Dawid-Skene)",
        "description": "Aggregates categorical labels from completed assignments to obtain ground truth using Dawid-Skene algorithm",
        "icon": "icon-puzzle-piece"
    },

    "kind": "PYTHON",
    
    "inputRoles": [
        {
            "name": "input_dataset",
            "label": "Input dataset",
            "description": "Dataset to input raw assignments",
            "arity": "UNARY",
            "required": true
        }
    ],

    "outputRoles": [
        {
            "name": "output_dataset",
            "label": "Output dataset",
            "description": "A dataset to store aggregated categories as ground truth",
            "arity": "UNARY",
            "required": true
        }
    ],

    "params": [
        {
            "name": "n_iter",
            "label": "Number of iterations",
            "type": "INT",
            "description": "Number of iterations, Dawid-Skene algorithm parameter.",
            "minI": 1,
            "default": 20
        }
    ],

    "resourceKeys": []
}
