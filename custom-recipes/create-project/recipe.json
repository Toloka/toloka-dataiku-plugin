{
    "meta": {
        "label": "Create project",
        "description": "Creates Toloka project",
        "icon": "icon-puzzle-piece"
    },

    "kind": "PYTHON",
    
    "selectableFromFolder": "input_folder",
    "inputRoles": [
        {
            "name": "input_folder",
            "label": "Input Folder",
            "description": "A folder containing the project configuration file",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": false,
            "acceptsManagedFolder": true
        }
    ],

    "outputRoles": [
        {
            "name": "output_folder",
            "label": "Output folder",
            "description": "A folder to write created project configuration file",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": false,
            "acceptsManagedFolder": true
        }
    ],

    "params": [
        {
            "name": "input_config_filename",
            "label": "Input config filename",
            "type": "STRING",
            "description": "JSON file name with project configuration in input folder",
            "defaultValue": "project.json",
            "mandatory": true
        },
        {
            "name": "output_project_config_filename",
            "label": "Output project config filename",
            "type": "STRING",
            "description": "JSON file to output created project configuration",
            "defaultValue": "project.json",
            "mandatory": true
        }
    ],

    "resourceKeys": []
}
