{
    "meta": {
        "label": "Open pool",
        "description": "Opens Toloka pool",
        "icon": "icon-puzzle-piece"
    },

    "kind": "PYTHON",
    
    "selectableFromFolder": "input_folder",
    "inputRoles": [
        {
            "name": "input_folder",
            "label": "Input Folder",
            "description": "A folder containing the created pool or training configuration file",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": false,
            "acceptsManagedFolder": true
        }
    ],

    "outputRoles": [
        {
            "name": "output_folder",
            "label": "output folder",
            "description": "A folder to write an opened pool or training configuration file",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": false,
            "acceptsManagedFolder": true
        }
    ],

    "params": [
        {
            "name": "input_config_filename",
            "label": "Input config filename",
            "type": "STRING",
            "description": "JSON file name with created pool/training config",
            "defaultValue": "pool.json",
            "mandatory": true
        },
        {
            "name": "output_pool_config_filename",
            "label": "Output pool config filename",
            "type": "STRING",
            "description": "JSON file name to write opened pool or training configuration file",
            "defaultValue": "pool.json",
            "mandatory": true
        }
    ],

    "resourceKeys": []
}
